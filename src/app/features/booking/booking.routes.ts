import { Routes } from '@angular/router';
import { BookingComponent } from './booking.component';

export const BOOKING_ROUTES: Routes = [
  {
    path: '',
    component: BookingComponent,
    title: 'Đặt vé - Flight Booking'
  }
]; 